// number => 2 to pow 53
const accountId = 12453

// bigInt

// string => "" / ''
let accountEmail = "ainain@google.com"
var accountPassword = "123@ainain"
accountCity = 'gaya'

// boolean  => true / false
let isLoggedIn = true

// undefined => variable is not initialized, or value is not defined
let accountState

// null => standalone value
let accountCountry = null;  // type object

console.log(typeof(accountId))
console.table([typeof(accountId), typeof(accountEmail), typeof(accountPassword), typeof(isLoggedIn), typeof(accountCity), typeof(accountState), typeof(accountCountry)])

// symbol => unique

// object

/*          <--- Important for interview --->
    console.log(typeof undefined)    // undefined
    console.log(typeof null)         // object 
*/
console.log(typeof accountState)    
console.log(typeof accountCountry)   